# Changelog - 260224

## 260224-04:05:17 - Land Lisa orchestration feature cluster and contract sync

### Summary
Implemented and validated the Lisa multi-agent orchestration feature set, then aligned runtime contracts, tests, help, usage docs, and installed skill metadata.

### Added
- New `session turn` command for one-shot `send -> monitor -> packet` orchestration.
- New `session state-sandbox` command set (`list|snapshot|restore|clear`) for objective/lane registry state control.
- Handoff schema `v4` support with typed `nextAction.commandAst` and deterministic identifiers.
- Smoke `--contract-profile full` coverage path.

### Changed
- `session route --strict` validation and typed `--from-state` handling.
- Strict `commandAst.args` acceptance for both array and object shorthand forms.
- Monitor/capture/packet/budget command surfaces and capability contracts (`--adaptive-poll`, `--strip-banner`, `--delta-json`, `--from-jsonl`).
- Help/capabilities/USAGE/skill contract docs aligned with current runtime behavior.

### Fixed
- Budget extraction now accepts numeric `steps` values from mixed JSONL payloads.
- Contract drift in skill command catalog and test gaps around `session turn` validation/json-min failure behavior.

### Files
- `src/commands_session_turn.go`
- `src/commands_session_state_sandbox.go`
- `src/commands_session_orchestration.go`
- `src/commands_session_governance.go`
- `src/commands_session_state.go`
- `src/commands_session_packet.go`
- `src/commands_session_smoke.go`
- `src/commands_capabilities.go`
- `src/help.go`
- `src/session_turn_state_sandbox_test.go`
- `src/session_route_handoff_cluster_a_test.go`
- `src/session_feature_cluster_b_test.go`
- `src/session_smoke_contract_profile_test.go`
- `USAGE.md`
- `skills/lisa/SKILL.md`
- `skills/lisa/data/commands.md`

### QA Notes
- Verify `./lisa session turn` success and failure contracts (`--json` and `--json-min`) in a live session.
- Verify `./lisa session route --strict --from-state` with both `commandAst.args` array and object shorthand payloads.
- Verify `./lisa session contract-check --project-root . --json` and `./lisa skills doctor --repo-root . --contract-check --json` both return `ok:true`.
- Verify `go test ./src` passes and no contract regressions appear in smoke with `--contract-profile full`.
